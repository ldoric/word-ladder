# Copy this file to `.env.local` in the repo root and replace the placeholder.
# `.env.local` is gitignored — do not commit it.
#
#   cp .env.local.example .env.local
#
# Windows (PowerShell): Copy_Item .env.local.example .env.local

OPENAI_API_KEY=sk-replace-me
GEMINI_API_KEY=replace-me
